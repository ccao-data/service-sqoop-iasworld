CREATE TABLE `iasworld.lpnbhd`(
  `jur` varchar,
  `ver` varchar,
  `nbhd` varchar,
  `seq` decimal,
  `cur` varchar,
  `who` varchar,
  `wen` string,
  `describ` varchar,
  `landfact` decimal,
  `bldgfact` decimal,
  `dwelfact` decimal,
  `obyfact` decimal,
  `ngroup` decimal,
  `grade` varchar,
  `cdu` varchar,
  `resmod` decimal,
  `lcodemod` decimal,
  `lotmod` decimal,
  `sfmod` decimal,
  `acmod` decimal,
  `grmod` decimal,
  `unmod` decimal,
  `agmod` decimal,
  `lotpct` decimal,
  `sfpct` decimal,
  `acpct` decimal,
  `grpct` decimal,
  `unpct` decimal,
  `agpct` decimal,
  `grm` decimal,
  `grmflg` varchar,
  `depthtble` varchar,
  `deprt` varchar,
  `cdutble` varchar,
  `valarea` varchar,
  `note1` varchar,
  `note2` varchar,
  `note3` varchar,
  `note4` varchar,
  `note5` varchar,
  `note6` varchar,
  `user1` varchar,
  `user2` varchar,
  `user3` varchar,
  `user4` varchar,
  `user5` varchar,
  `user6` varchar,
  `user7` varchar,
  `user8` varchar,
  `user9` varchar,
  `user10` varchar,
  `user11` varchar,
  `user12` varchar,
  `user13` varchar,
  `user14` varchar,
  `user15` varchar,
  `user16` varchar,
  `user17` varchar,
  `user18` varchar,
  `user19` varchar,
  `user20` varchar,
  `deactivat` string,
  `rent` decimal,
  `iasw_id` decimal,
  `revmeth` varchar,
  `landpct` decimal,
  `modelid` decimal,
  `loaded_at` string)
STORED AS PARQUET
TBLPROPERTIES ('parquet.compression'='ZSTD');

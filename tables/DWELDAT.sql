CREATE TABLE `iasworld.dweldat`(
  `jur` varchar,
  `parid` varchar,
  `card` decimal,
  `seq` decimal,
  `cur` varchar,
  `who` varchar,
  `wen` string,
  `whocalc` varchar,
  `wencalc` string,
  `status` varchar,
  `stories` decimal,
  `extwall` varchar,
  `style` varchar,
  `yrblt` decimal,
  `effyr` decimal,
  `yrremod` decimal,
  `rmtot` decimal,
  `rmbed` decimal,
  `rmfam` decimal,
  `fixbath` decimal,
  `fixhalf` decimal,
  `fixaddl` decimal,
  `fixtot` decimal,
  `plumval` decimal,
  `remkit` varchar,
  `rembath` varchar,
  `bsmt` varchar,
  `bsmtval` decimal,
  `heat` varchar,
  `fuel` varchar,
  `heatsys` varchar,
  `heatval` decimal,
  `attic` varchar,
  `atticval` decimal,
  `intext` varchar,
  `mastrimw` decimal,
  `mastriml` decimal,
  `mastrimarea` decimal,
  `trimval` decimal,
  `unfinw` decimal,
  `unfinl` decimal,
  `unfinarea` decimal,
  `unfinval` decimal,
  `recromw` decimal,
  `recroml` decimal,
  `recromarea` decimal,
  `recval` decimal,
  `finbsmtw` decimal,
  `finbsmtl` decimal,
  `finbsmtarea` decimal,
  `finbsmtval` decimal,
  `ufeatw` decimal,
  `ufeatl` decimal,
  `ufeatarea` decimal,
  `ufeatval` decimal,
  `wbfp_o` decimal,
  `wbfp_s` decimal,
  `wbfp_x` decimal,
  `wbfp_pf` decimal,
  `wbfp_pfx` decimal,
  `wbfpval` decimal,
  `bsmtcar` decimal,
  `bgarval` decimal,
  `msc1des` varchar,
  `msc1num` decimal,
  `msc1val` decimal,
  `msc2des` varchar,
  `msc2num` decimal,
  `msc2val` decimal,
  `condolvl` decimal,
  `condotyp` varchar,
  `condovw` varchar,
  `cndcmplx` varchar,
  `cline` decimal,
  `unitno` varchar,
  `cndbaseval` decimal,
  `cndadjval` decimal,
  `grmunits` decimal,
  `grmrent` decimal,
  `adjgrmfact` decimal,
  `resgrmval` decimal,
  `mgfa` decimal,
  `sfla` decimal,
  `flr1area` decimal,
  `adjarea` decimal,
  `areafact` decimal,
  `shfact` decimal,
  `cnstfact` decimal,
  `cnstval` decimal,
  `subtval` decimal,
  `grade` varchar,
  `grdfact` decimal,
  `cddesc` varchar,
  `cdpct` decimal,
  `userfact` decimal,
  `useramt` decimal,
  `locmult` decimal,
  `basercn` decimal,
  `rcnval` decimal,
  `cdu` varchar,
  `depr` decimal,
  `mktadj` decimal,
  `mktrsn` varchar,
  `basercnld` decimal,
  `addnarea` decimal,
  `addnrcnval` decimal,
  `resfeatval` decimal,
  `addnval` decimal,
  `rcnld` decimal,
  `adjfact` decimal,
  `pctcomplete` decimal,
  `newconmo` varchar,
  `newconpct` decimal,
  `adjrcnld` decimal,
  `flr1val` decimal,
  `flruarea` decimal,
  `flruval` decimal,
  `flrharea` decimal,
  `flrhval` decimal,
  `bsmtarea` decimal,
  `atticarea` decimal,
  `catharea` decimal,
  `cathval` decimal,
  `slabarea` decimal,
  `slabval` decimal,
  `heatarea` decimal,
  `acarea` decimal,
  `acval` decimal,
  `rmbedb` decimal,
  `rmbed1` decimal,
  `rmbed2` decimal,
  `rmbed3` decimal,
  `fixbathb` decimal,
  `fixbath1` decimal,
  `fixbath2` decimal,
  `fixbath3` decimal,
  `fixhalfb` decimal,
  `fixhalf1` decimal,
  `fixhalf2` decimal,
  `fixhalf3` decimal,
  `rmkitb` decimal,
  `rmkit1` decimal,
  `rmkit2` decimal,
  `rmkit3` decimal,
  `rmkit` decimal,
  `rmlivb` decimal,
  `rmliv1` decimal,
  `rmliv2` decimal,
  `rmliv3` decimal,
  `rmliv` decimal,
  `rmdinb` decimal,
  `rmdin1` decimal,
  `rmdin2` decimal,
  `rmdin3` decimal,
  `rmdin` decimal,
  `rmfamb` decimal,
  `rmfam1` decimal,
  `rmfam2` decimal,
  `rmfam3` decimal,
  `rmothb` decimal,
  `rmoth1` decimal,
  `rmoth2` decimal,
  `rmoth3` decimal,
  `rmoth` decimal,
  `note1` varchar,
  `note2` varchar,
  `class` varchar,
  `degrem` varchar,
  `cond` varchar,
  `valmeth` varchar,
  `rectype` varchar,
  `resmod` decimal,
  `modover` decimal,
  `exmppctover` decimal,
  `exmppct` decimal,
  `exmpval` decimal,
  `areasum` decimal,
  `obsdep` decimal,
  `obsrsn` varchar,
  `fundep` decimal,
  `funrsn` varchar,
  `ecndep` decimal,
  `ecnrsn` varchar,
  `bldguse` varchar,
  `convbldg` decimal,
  `chgrsn` varchar,
  `deprt` varchar,
  `user1` varchar,
  `user2` varchar,
  `user3` varchar,
  `user4` varchar,
  `user5` varchar,
  `user6` varchar,
  `user7` varchar,
  `user8` varchar,
  `user9` varchar,
  `user10` varchar,
  `user11` varchar,
  `user12` varchar,
  `user13` varchar,
  `user14` varchar,
  `user15` varchar,
  `user16` varchar,
  `user17` varchar,
  `user18` varchar,
  `user19` varchar,
  `user20` varchar,
  `deactivat` string,
  `salekey` decimal,
  `iasw_id` decimal,
  `trans_id` decimal,
  `upd_status` varchar,
  `prodate` string,
  `protype` varchar,
  `prodamage` decimal,
  `profact` decimal,
  `user21` varchar,
  `user22` varchar,
  `user23` varchar,
  `user24` varchar,
  `user25` varchar,
  `user26` varchar,
  `user27` varchar,
  `user28` varchar,
  `user29` varchar,
  `user30` varchar,
  `user31` varchar,
  `user32` varchar,
  `user33` varchar,
  `user34` varchar,
  `user35` varchar,
  `user36` varchar,
  `user37` varchar,
  `user38` varchar,
  `user39` varchar,
  `user40` varchar,
  `msc3des` varchar,
  `msc3num` decimal,
  `msc3val` decimal,
  `msc4des` varchar,
  `msc4num` decimal,
  `msc4val` decimal,
  `msc5des` varchar,
  `msc5num` decimal,
  `msc5val` decimal,
  `msc6des` varchar,
  `msc6num` decimal,
  `msc6val` decimal,
  `msc7des` varchar,
  `msc7num` decimal,
  `msc7val` decimal,
  `msc8des` varchar,
  `msc8num` decimal,
  `msc8val` decimal,
  `msc9des` varchar,
  `msc9num` decimal,
  `msc9val` decimal,
  `msc10des` varchar,
  `msc10num` decimal,
  `msc10val` decimal,
  `msc11des` varchar,
  `msc11num` decimal,
  `msc11val` decimal,
  `msc12des` varchar,
  `msc12num` decimal,
  `msc12val` decimal,
  `effyrovr` decimal,
  `sidpct` decimal,
  `sidyr` decimal,
  `winpct` decimal,
  `winyr` decimal,
  `furpct` decimal,
  `furyr` decimal,
  `elepct` decimal,
  `eleyr` decimal,
  `plupct` decimal,
  `pluyr` decimal,
  `kitpct` decimal,
  `kityr` decimal,
  `estpct` decimal,
  `estyr` decimal,
  `calceffyr` decimal,
  `nccalc` decimal,
  `ncoval` decimal,
  `ncval` decimal,
  `nctot` decimal,
  `hga` varchar,
  `recnr` decimal,
  `ovrrcnld` decimal,
  `condosflaovr` decimal,
  `grmovr` decimal,
  `fixbath4` decimal,
  `fixbath5` decimal,
  `fixbath6` decimal,
  `fixbath7` decimal,
  `ovrrcn` decimal,
  `lumpcure` decimal,
  `numident` decimal,
  `grpadj` decimal,
  `stage` decimal,
  `excess` varchar,
  `suppress` varchar,
  `lumpcamod` decimal,
  `bld_modelid` varchar,
  `ovrmraval` decimal,
  `renolvl` varchar,
  `prior_renolvl` varchar,
  `renopoint` decimal,
  `renoovr` varchar,
  `renoyr` decimal,
  `mvpbsmt` decimal,
  `mvpunfin` decimal,
  `mvpsf` decimal,
  `mvpattic` decimal,
  `rcnfact` decimal,
  `udate1` string,
  `udate2` string,
  `udate3` string,
  `udate4` string,
  `udate5` string,
  `mvpstyle` varchar,
  `accode` varchar,
  `eff_area` decimal,
  `baserate` decimal,
  `adjrate` decimal,
  `ac` varchar,
  `extwall1` varchar,
  `extwall1pct` decimal,
  `effageovr` decimal,
  `ovrnbhd` varchar,
  `intwall` varchar,
  `intwall1` varchar,
  `intwall1pct` decimal,
  `intwallfact` decimal,
  `floor` varchar,
  `floor1` varchar,
  `floor1pct` decimal,
  `floorfact` decimal,
  `sizecode` varchar,
  `wallhgt` decimal,
  `wallhgtfact` decimal,
  `comwallpct` decimal,
  `comwallfact` decimal,
  `ceiling` varchar,
  `addfact` decimal,
  `fixtotovr` decimal,
  `fixhalfm` decimal,
  `fixhalfu` decimal,
  `fixbathm` decimal,
  `fixbathu` decimal,
  `fixbath4m` decimal,
  `fixbath4u` decimal,
  `fixbath5m` decimal,
  `fixbath5u` decimal,
  `plumbgrade` varchar,
  `sqmla` decimal,
  `mgfa_sqm` decimal,
  `flr1area_sqm` decimal,
  `addnarea_sqm` decimal,
  `external_rcnld` decimal,
  `external_occpct` decimal,
  `external_propct` decimal,
  `external_calc_rcnld` decimal,
  `calc_meth` varchar,
  `note3` string,
  `loaded_at` string)
PARTITIONED BY (`taxyr` string)
STORED AS PARQUET
TBLPROPERTIES ('parquet.compression'='ZSTD');

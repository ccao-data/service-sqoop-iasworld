CREATE TABLE `iasworld.addrindx`(
  `jur` varchar,
  `parid` varchar,
  `card` decimal,
  `lline` decimal,
  `tble` varchar,
  `tabseq` decimal,
  `seq` decimal,
  `cur` varchar,
  `who` varchar,
  `wen` string,
  `adrpre` varchar,
  `adrno` decimal,
  `adrgrid` varchar,
  `adradd` varchar,
  `adrdir` varchar,
  `adrstr` varchar,
  `adrsuf` varchar,
  `adrsuf2` varchar,
  `cityname` varchar,
  `statecode` varchar,
  `unitdesc` varchar,
  `unitno` varchar,
  `zip1` varchar,
  `zip2` varchar,
  `loc2` varchar,
  `defaddr` varchar,
  `deactivat` string,
  `iasw_id` decimal,
  `trans_id` decimal,
  `upd_status` varchar,
  `gislink` varchar,
  `bldgno` varchar,
  `childparid` varchar,
  `user1` varchar,
  `user2` varchar,
  `user3` varchar,
  `user4` varchar,
  `user5` varchar,
  `user6` varchar,
  `user7` varchar,
  `user8` varchar,
  `user9` varchar,
  `user10` varchar,
  `user11` varchar,
  `user12` varchar,
  `user13` varchar,
  `user14` varchar,
  `user15` varchar,
  `adrid` decimal,
  `adrparchild` varchar,
  `adrstatus` varchar,
  `adrpremod` varchar,
  `adrpretype` varchar,
  `adrpostmod` varchar,
  `floorno` varchar,
  `coopid` varchar,
  `country` varchar,
  `postalcode` varchar,
  `addrsrc` varchar,
  `addrvalid` varchar,
  `xcoord` decimal,
  `ycoord` decimal,
  `strcd` varchar,
  `strreloc` varchar,
  `loaded_at` string)
PARTITIONED BY (`taxyr` string)
STORED AS PARQUET
TBLPROPERTIES ('parquet.compression'='ZSTD');

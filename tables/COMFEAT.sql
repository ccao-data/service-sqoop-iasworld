CREATE TABLE `iasworld.comfeat`(
  `jur` varchar,
  `parid` varchar,
  `card` decimal,
  `lline` decimal,
  `seq` decimal,
  `cur` varchar,
  `who` varchar,
  `wen` string,
  `whocalc` varchar,
  `wencalc` string,
  `status` varchar,
  `lnum` decimal,
  `struct` varchar,
  `ofsign` varchar,
  `meas1` decimal,
  `meas2` decimal,
  `stops` decimal,
  `ofnum` decimal,
  `price` decimal,
  `areasum` decimal,
  `rectype` varchar,
  `vect` varchar,
  `chgrsn` varchar,
  `user1` varchar,
  `user2` varchar,
  `user3` varchar,
  `user4` varchar,
  `user5` varchar,
  `deactivat` string,
  `salekey` decimal,
  `iasw_id` decimal,
  `trans_id` decimal,
  `upd_status` varchar,
  `vectnotes` varchar,
  `areaid` decimal,
  `area` decimal,
  `meas3` decimal,
  `msbsystemcode` varchar,
  `comppct` decimal,
  `rate` decimal,
  `rcn` decimal,
  `meas4` decimal,
  `meas5` decimal,
  `meas6` decimal,
  `msrank` decimal,
  `units` decimal,
  `altbasedate` string,
  `rateovr` decimal,
  `convbldg` decimal,
  `meas1_m` decimal,
  `meas2_m` decimal,
  `meas3_m` decimal,
  `meas4_m` decimal,
  `meas5_m` decimal,
  `loaded_at` string)
PARTITIONED BY (`taxyr` string)
STORED AS PARQUET
TBLPROPERTIES ('parquet.compression'='ZSTD');

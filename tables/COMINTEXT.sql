CREATE TABLE `iasworld.comintext`(
  `jur` varchar,
  `parid` varchar,
  `card` decimal,
  `lline` decimal,
  `seq` decimal,
  `cur` varchar,
  `who` varchar,
  `wen` string,
  `whocalc` varchar,
  `wencalc` string,
  `status` varchar,
  `sect` varchar,
  `flrfrom` varchar,
  `flrto` varchar,
  `lngth` decimal,
  `width` decimal,
  `area` decimal,
  `perim` decimal,
  `stories` decimal,
  `sf` decimal,
  `cubicft` decimal,
  `usetype` varchar,
  `wallhgt` decimal,
  `extwall` varchar,
  `constr` varchar,
  `intfin` decimal,
  `intwall` varchar,
  `heat` varchar,
  `air` varchar,
  `plumb` varchar,
  `light` varchar,
  `rate` decimal,
  `storyadj` decimal,
  `lineval` decimal,
  `rcn` decimal,
  `featval` decimal,
  `useradjovr` decimal,
  `mktadj` decimal,
  `phycond` varchar,
  `functutil` varchar,
  `depr` decimal,
  `pctcomp` decimal,
  `newconmo` varchar,
  `newconpct` decimal,
  `price` decimal,
  `class` varchar,
  `yrblt` decimal,
  `effyr` decimal,
  `obsdep` decimal,
  `obsrsn` varchar,
  `fundep` decimal,
  `funrsn` varchar,
  `ecndep` decimal,
  `ecnrsn` varchar,
  `rentpct` decimal,
  `rentsf` decimal,
  `areasum` decimal,
  `incuse` varchar,
  `mssect` varchar,
  `mstab` varchar,
  `occupancy` decimal,
  `splevel` varchar,
  `msclass` varchar,
  `msheat` varchar,
  `msunits` decimal,
  `qualtype` varchar,
  `ovrstories` decimal,
  `baserate` decimal,
  `elevrate` decimal,
  `heatrate` decimal,
  `parmult` decimal,
  `shmult` decimal,
  `nsmult` decimal,
  `curmult` decimal,
  `locmult` decimal,
  `rectype` varchar,
  `exmppct` decimal,
  `exmpval` decimal,
  `vect` varchar,
  `chgrsn` varchar,
  `user1` varchar,
  `user2` varchar,
  `user3` varchar,
  `user4` varchar,
  `user5` varchar,
  `deactivat` string,
  `salekey` decimal,
  `iasw_id` decimal,
  `trans_id` decimal,
  `upd_status` varchar,
  `prodate` string,
  `protype` varchar,
  `prodamage` decimal,
  `profact` decimal,
  `vectnotes` varchar,
  `areaid` decimal,
  `occupancypct` decimal,
  `msrank` decimal,
  `extwallpct` decimal,
  `extwallrate` decimal,
  `extwallrcn` decimal,
  `extwallprice` decimal,
  `msheatpct` decimal,
  `msheatrcn` decimal,
  `msheatprice` decimal,
  `mssect1` varchar,
  `mstab1` varchar,
  `occupancy1` decimal,
  `occupancy1pct` decimal,
  `splevel1` varchar,
  `msclass1` varchar,
  `msrank1` decimal,
  `qualtype1` varchar,
  `extwall1` varchar,
  `extwall1pct` decimal,
  `extwall1rate` decimal,
  `extwall1rcn` decimal,
  `extwall1price` decimal,
  `msheat1` varchar,
  `msheat1pct` decimal,
  `heat1rate` decimal,
  `msheat1rcn` decimal,
  `msheat1price` decimal,
  `bsmtsectassociation` decimal,
  `bsmtfireproofflg` varchar,
  `nccalc` decimal,
  `ncoval` decimal,
  `ncval` decimal,
  `nctot` decimal,
  `tenants` decimal,
  `lodgepct` decimal,
  `lodgeclass` varchar,
  `lumpdepr` decimal,
  `lumpcure` decimal,
  `deprt` varchar,
  `ovrrcn` decimal,
  `altbasedate` string,
  `ovrrcnld` decimal,
  `adjrcn` decimal,
  `lcf` decimal,
  `adjrcnld` decimal,
  `calcexclude` varchar,
  `grpadj` decimal,
  `excess` varchar,
  `suppress` varchar,
  `adjfact` decimal,
  `lumpcamod` decimal,
  `convbldg` decimal,
  `lngth_m` decimal,
  `width_m` decimal,
  `wallhgt_m` decimal,
  `perim_m` decimal,
  `area_sqm` decimal,
  `user6` varchar,
  `user7` varchar,
  `user8` varchar,
  `user9` varchar,
  `user10` varchar,
  `user11` varchar,
  `user12` varchar,
  `user13` varchar,
  `user14` varchar,
  `user15` varchar,
  `user16` varchar,
  `user17` varchar,
  `user18` varchar,
  `user19` varchar,
  `user20` varchar,
  `udate1` string,
  `udate2` string,
  `udate3` string,
  `udate4` string,
  `udate5` string,
  `userval1` decimal,
  `userval2` decimal,
  `userval3` decimal,
  `userval4` decimal,
  `userval5` decimal,
  `userval6` decimal,
  `userval7` decimal,
  `userval8` decimal,
  `userval9` decimal,
  `userval10` decimal,
  `userval11` decimal,
  `userval12` decimal,
  `userval13` decimal,
  `userval14` decimal,
  `userval15` decimal,
  `segmod` varchar,
  `sprinkler_pct` decimal,
  `alarm_pct` decimal,
  `ac_pct` decimal,
  `walla_pct` decimal,
  `wallb_pct` decimal,
  `wallc_pct` decimal,
  `bsmtarea` decimal,
  `bsmtperm` decimal,
  `bsmttype` varchar,
  `no_elev` decimal,
  `wallarea` decimal,
  `slabarea` decimal,
  `structarea` decimal,
  `cubicfeet` decimal,
  `ac_units` decimal,
  `user21` varchar,
  `user22` varchar,
  `user23` varchar,
  `user24` varchar,
  `user25` varchar,
  `numident` decimal,
  `effageovr` decimal,
  `upperflarea` decimal,
  `loaded_at` string)
PARTITIONED BY (`taxyr` string)
STORED AS PARQUET
TBLPROPERTIES ('parquet.compression'='ZSTD');

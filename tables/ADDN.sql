CREATE TABLE `iasworld.addn`(
  `jur` varchar,
  `parid` varchar,
  `card` decimal,
  `lline` decimal,
  `seq` decimal,
  `cur` varchar,
  `who` varchar,
  `wen` string,
  `whocalc` varchar,
  `wencalc` string,
  `status` varchar,
  `lower` varchar,
  `first` varchar,
  `second` varchar,
  `third` varchar,
  `area` decimal,
  `vect` varchar,
  `value` decimal,
  `class` varchar,
  `pctcomp` decimal,
  `newconmo` varchar,
  `newconpct` decimal,
  `yrblt` decimal,
  `effyr` decimal,
  `grade` varchar,
  `grdfact` decimal,
  `cdu` varchar,
  `depr` decimal,
  `mktadj` decimal,
  `rcnld` decimal,
  `rectype` varchar,
  `areasum` decimal,
  `exmppct` decimal,
  `exmpval` decimal,
  `ratel` decimal,
  `rate1` decimal,
  `rate2` decimal,
  `rate3` decimal,
  `grdfactl` decimal,
  `grdfact1` decimal,
  `grdfact2` decimal,
  `grdfact3` decimal,
  `rcnl` decimal,
  `rcn1` decimal,
  `rcn2` decimal,
  `rcn3` decimal,
  `rcnldl` decimal,
  `rcnld1` decimal,
  `rcnld2` decimal,
  `rcnld3` decimal,
  `convaddn` decimal,
  `chgrsn` varchar,
  `deactivat` string,
  `salekey` decimal,
  `iasw_id` decimal,
  `trans_id` decimal,
  `upd_status` varchar,
  `prodate` string,
  `protype` varchar,
  `prodamage` decimal,
  `profact` decimal,
  `vectnotes` varchar,
  `areaid` decimal,
  `nccalc` decimal,
  `ncoval` decimal,
  `ncval` decimal,
  `nctot` decimal,
  `user1` varchar,
  `user2` varchar,
  `user3` varchar,
  `user4` varchar,
  `user5` varchar,
  `userval1` decimal,
  `userval2` decimal,
  `userval3` decimal,
  `userval4` decimal,
  `userval5` decimal,
  `area_sqm` decimal,
  `eff_area` decimal,
  `effageovr` decimal,
  `stories` decimal,
  `footprint` decimal,
  `loaded_at` string)
PARTITIONED BY (`taxyr` string)
STORED AS PARQUET
TBLPROPERTIES ('parquet.compression'='ZSTD');
